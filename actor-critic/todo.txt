agents:
    - add a (class?)method to return an object, than can easily be stored by
        some IO function

env:
    - literally everything
    - additionally the necessary functions step, reset, .. see gym
    - print warnings.warn messages if undesired behaviour is happening!

plots:
    - masked arrays -> empty cells are white (ma.masked_equal(arr, 0))

 profiler:
    - use one!
