agents:
    - add a (class?)method to return an object, than can easily be stored by
        some IO function
    - advance the __str__ magic method!

env:
    - additionally the necessary functions step, reset, seed .. see gym
    - conversion method to convert neighbourhood to array of ints
    - one or two class constants containing the rewards

plots:
    - masked arrays -> empty cells are white (ma.masked_equal(arr, 0))

 profiler:
    - use one!

simulation:
    - how to store data during runtime? -> with open .....
    - if a predator gets to eat a prey, the prey agent should be next in line
    - write a proper yaml configuration file

documentation:
    - cleanup of existing documentation
    - write new documentation for environment

general:
    - cythonize the code?
